import { StyleSheet, TextBase } from 'react-native';

export default StyleSheet.create({
    mainContainer: {
        flex: 1
    },
    container: {
        // flex: 1,
        flexDirection: "row"
    }
}) 