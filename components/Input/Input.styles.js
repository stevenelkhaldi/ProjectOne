import { StyleSheet, StatusBar } from 'react-native';

export default  StyleSheet.create({
    mainContainer: {
        margin: 10
    },
    container: {
        borderColor: 'red',
        borderWidth: 1,
        width: '50%',
        height: 30
    }
})